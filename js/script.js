//FUNÇÃO DE ROLAGAM DA HOME

function irParaSessaoHome() {
    // Obtém a referência para a primeira instância da classe de destino
    var sessoes = document.getElementsByClassName("inter-presentation");

    // Verifica se há pelo menos uma instância da classe
    if (sessoes.length > 0) {
        // Rola a página até a posição da primeira instância da classe
        sessoes[0].scrollIntoView({ behavior: "smooth" });
    }
}


//FUNÇÃO DE ROLAGAM DA SOBRE

function irParaSessaoSobre() {
    // Obtém a referência para a primeira instância da classe de destino
    var sessoes = document.getElementsByClassName("sobre-mim");

    // Verifica se há pelo menos uma instância da classe
    if (sessoes.length > 0) {
        // Rola a página até a posição da primeira instância da classe
        sessoes[0].scrollIntoView({ behavior: "smooth" });
    }
}



//FUNÇÃO DE ROLAGAM DA 

function irParaSessaoProjeto() {
    // Obtém a referência para a primeira instância da classe de destino
    var sessoes = document.getElementsByClassName("projetos");

    // Verifica se há pelo menos uma instância da classe
    if (sessoes.length > 0) {
        // Rola a página até a posição da primeira instância da classe
        sessoes[0].scrollIntoView({ behavior: "smooth" });
    }
}

//FUNÇÃO DE ROLAGAM DA HOME

function irParaSessaoContato() {
    // Obtém a referência para a primeira instância da classe de destino
    var sessoes = document.getElementsByClassName("upendereço");

    // Verifica se há pelo menos uma instância da classe
    if (sessoes.length > 0) {
        // Rola a página até a posição da primeira instância da classe
        sessoes[0].scrollIntoView({ behavior: "smooth" });
    }
}


            // BOTÃO MAIS SOBRE//
function irParabtnsobre() {
    // Obtém a referência para a primeira instância da classe de destino
    var sessoes = document.getElementsByClassName("bote-maissbore");

    // Verifica se há pelo menos uma instância da classe
    if (sessoes.length > 0) {
        // Rola a página até a posição da primeira instância da classe
        sessoes[0].scrollIntoView({ behavior: "smooth" });
    }
}
